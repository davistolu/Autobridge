// Re-export types from core manifest
export type {
  BackendManifest,
  BackendCapability,
  FrontendManifest,
  FrontendIntent,
  Contract,
  FieldSchema,
  ContractStatus,
  ContractSource,
} from '../../core/src/manifest/types.js';
